let start = document.querySelector ('input.start');
start.addEventListener ('click', startClicked);

let chronometer = document.querySelector ('p.chronometer');
//console.log (chronometer);

function startClicked () {
    console.log (`clicou`);
}